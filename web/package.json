{
  "name": "litesync-web",
  "version": "0.4.0",
  "private": true,
  "description": "LiteSync web readonly client",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node build.mjs"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "esbuild": "^0.25.0",
    "markdown-it": "^14.1.0",
    "typescript": "^5.7.0"
  }
}
